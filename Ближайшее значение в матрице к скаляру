'''
Найти ближайшее значение к заданному числу x в матрице
'''



import numpy as np
A = np.random.randint(-100, 100, 45).reshape(3, 5, 3)
x = np.random.uniform(-100, 100)
print(A, x)
flat_index = np.abs(A-x).argmin()
matrix_index = np.unravel_index(flat_index, A.shape)
print(A[matrix_index])
